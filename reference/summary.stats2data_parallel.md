# Summarize a stats2data parallel result

Summarize a stats2data parallel result

## Usage

``` r
# S3 method for class 'stats2data_parallel'
summary(object, ...)
```

## Arguments

- object:

  An object of class `stats2data_parallel`.

- ...:

  Additional arguments (unused).

## Value

An object of class `summary.stats2data_parallel`, printed by
`print.summary.stats2data_parallel`.
