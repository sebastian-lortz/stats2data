% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_optim_aov.R
\name{optim_aov}
\alias{optim_aov}
\title{Optimize simulated data to match ANOVA F-values}
\usage{
optim_aov(
  S,
  levels,
  target_group_means,
  target_f_list,
  integer,
  range,
  formula,
  factor_type,
  subgroup_sizes = NULL,
  tolerance = 0.005,
  max_iter = 5000,
  init_temp = 0.001,
  cooling_rate = NULL,
  max_starts = 3,
  progress_mode = "console"
)
}
\arguments{
\item{S}{Integer. Total number of subjects.}

\item{levels}{Integer vector. Number of levels per factor.}

\item{target_group_means}{Numeric vector of length \code{prod(levels)}.
Target cell means in \code{expand.grid} order.}

\item{target_f_list}{List with components \code{effect} (character) and
\code{F} (numeric) of equal length.}

\item{integer}{Logical. Generate integer-valued data?}

\item{range}{Numeric vector of length 2. Bounds for individual observations.}

\item{formula}{Formula or character. ANOVA model formula.}

\item{factor_type}{Character vector (\code{"between"}/\code{"within"})
matching length of \code{levels}.}

\item{subgroup_sizes}{Optional numeric vector of between-group sizes
(must sum to \code{N}).}

\item{tolerance}{Numeric. Convergence threshold. Default \code{1e-2}.}

\item{max_iter}{Integer. Iterations per restart. Default \code{1e3}.}

\item{init_temp}{Numeric. Initial SA temperature. Default \code{1}.}

\item{cooling_rate}{Numeric in (0,1) or \code{NULL} (auto). Default \code{NULL}.}

\item{max_starts}{Integer. Number of restarts. Default \code{1}.}

\item{progress_mode}{Character: \code{"console"}, \code{"shiny"}, or
\code{"off"}. Default \code{"console"}.}
}
\value{
A \code{nds3.object} list with components \code{best_error},
  \code{data}, \code{inputs}, \code{adjusted_targets}, and
  \code{track_error}.
}
\description{
Uses simulated annealing to generate raw data that reproduce target
ANOVA F-statistics and group means under a specified factorial design.
}
