% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_plot_error.R, R/generics.R
\name{plot_error.stats2data_aov}
\alias{plot_error.stats2data_aov}
\alias{plot_error.stats2data_mlr}
\alias{plot_error.stats2data_vec}
\alias{plot_error}
\title{Plot error trajectory for a stats2data result}
\usage{
\method{plot_error}{stats2data_aov}(x, run = 1L, show_best = TRUE, first_iter = 1L, ...)

\method{plot_error}{stats2data_mlr}(
  x,
  run = 1L,
  show_best = TRUE,
  first_iter = 1L,
  ratio = FALSE,
  show_mean = TRUE,
  show_median = TRUE,
  show_final = TRUE,
  ...
)

\method{plot_error}{stats2data_vec}(x, run = 1L, show_best = TRUE, first_iter = 1L, ...)

plot_error(x, ...)
}
\arguments{
\item{x}{A stats2data result object (\code{stats2data_aov},
\code{stats2data_mlr}, or \code{stats2data_vec}).}

\item{run}{Integer. Index of the run (variable) to plot; default \code{1}.}

\item{show_best}{Logical. If \code{TRUE} (default), marks the minimum
error.}

\item{first_iter}{Integer. Iterations to skip; default \code{1}.}

\item{...}{Arguments passed to methods (see individual method pages).}

\item{ratio}{Logical. If \code{TRUE}, plot the correlation-to-regression
error ratio instead of the total error trajectory. Default \code{FALSE}.}

\item{show_mean}{Logical. When \code{ratio = TRUE}, draw a horizontal line
at the mean ratio. Default \code{TRUE}. Ignored when \code{ratio = FALSE}.}

\item{show_median}{Logical. When \code{ratio = TRUE}, draw a horizontal
line at the median ratio. Default \code{TRUE}. Ignored when
\code{ratio = FALSE}.}

\item{show_final}{Logical. When \code{ratio = TRUE}, draw a horizontal
line at the final ratio. Default \code{TRUE}. Ignored when
\code{ratio = FALSE}.}
}
\value{
A \code{\link[ggplot2]{ggplot}} object, returned invisibly.

A \code{\link[ggplot2]{ggplot}} object, returned invisibly.

A \code{\link[ggplot2]{ggplot}} object, returned invisibly.

A \code{\link[ggplot2]{ggplot}} object, returned invisibly.
}
\description{
Visualizes how the objective-function error evolves across iterations of
the simulated-annealing optimiser.  Dispatches to class-specific methods
for \code{stats2data_aov}, \code{stats2data_mlr}, and \code{stats2data_vec}.
}
\details{
For \code{stats2data_mlr} objects, set \code{ratio = TRUE} to plot the
correlation-to-regression error ratio instead of the total error.
}
\section{Methods (by class)}{
\itemize{
\item \code{plot_error(stats2data_aov)}: Method for ANOVA results (\code{stats2data_aov}).

\item \code{plot_error(stats2data_mlr)}: Method for MLR results (\code{stats2data_mlr}).

\item \code{plot_error(stats2data_vec)}: Method for Descriptives results
(\code{stats2data_vec}).

}}
\examples{
\dontrun{
res <- optim_aov(...)
plot_error(res)
}

\dontrun{
res <- optim_mlr(...)
plot_error(res)
plot_error(res, ratio = TRUE)
plot_error(res, ratio = TRUE, show_mean = FALSE)
}

\dontrun{
res <- optim_vec(...)
plot_error(res)
}

\dontrun{
res <- optim_mlr(...)
plot_error(res)
plot_error(res, ratio = TRUE)
plot_error(res, run = 2, first_iter = 500)
}

}
\seealso{
\code{\link{plot_error.stats2data_aov}},
  \code{\link{plot_error.stats2data_mlr}},
  \code{\link{plot_error.stats2data_vec}},
  \code{\link{plot_cooling}}
}
