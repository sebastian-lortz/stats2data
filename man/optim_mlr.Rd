% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_optim_mlr.R
\name{optim_mlr}
\alias{optim_mlr}
\title{Optimize simulated data to match target correlations and regression estimates}
\usage{
optim_mlr(
  N,
  target_mean,
  target_sd,
  range,
  integer,
  target_cor,
  target_reg,
  reg_equation,
  sprite_prec = c(2, 2),
  target_se = NULL,
  weight = 0.5,
  thresh = 0.005,
  max_iter = 1e+05,
  init_temp = NULL,
  cooling_rate = NULL,
  max_starts = 3,
  hill_climbs = 10000,
  progress_mode = "console"
)
}
\arguments{
\item{N}{Integer. Total number of observations.}

\item{target_mean}{Named numeric vector. Desired means for each variable
(names must match variables in \code{reg_equation}).}

\item{target_sd}{Named numeric vector. Desired standard deviations for each
variable (same length and names as \code{target_mean}).}

\item{range}{Numeric vector of length 2 or numeric matrix. Allowed value
range for all variables (vector), or per-variable bounds as a two-row
matrix with columns matching \code{target_mean}.}

\item{integer}{Logical or logical vector. If TRUE, generate integer-valued
data; length 1 or same length as \code{target_mean}.}

\item{target_cor}{Numeric vector. Target upper-triangular (excluding
diagonal) correlation values for predictor and outcome variables.}

\item{target_reg}{Numeric vector. Target regression coefficients including
intercept, matching terms in \code{reg_equation}.}

\item{reg_equation}{Character. Regression model formula
(e.g., \code{"Y ~ X1 + X2 + X1:X2"}).}

\item{sprite_prec}{Integer vector of length 2. Decimal precision for mean
and SD when using SPRITE for integer data. Default \code{c(2, 2)}.}

\item{target_se}{Numeric vector or NULL. Target standard errors for
regression coefficients (same length as \code{target_reg}).
Default \code{NULL}.}

\item{weight}{Numeric vector of length 2. Weights for correlation vs.
regression error in the objective function. Default \code{c(1, 1)}.}

\item{thresh}{Numeric. Convergence threshold. Default \code{1e-6}.}

\item{max_iter}{Integer. Iterations per restart. Default \code{1e5}.}

\item{init_temp}{Numeric. Initial SA temperature. Default \code{1}.}

\item{cooling_rate}{Numeric in (0,1) or \code{NULL} (auto).
Default \code{NULL}.}

\item{max_starts}{Integer. Number of restarts. Default \code{1}.}

\item{hill_climbs}{Integer or NULL. Number of hill-climbing iterations for
optional local refinement; if NULL, skips refinement. Default \code{NULL}.}

\item{progress_mode}{Character: \code{"console"}, \code{"shiny"}, or
\code{"off"}. Default \code{"console"}.}
}
\value{
A \code{stats2data.object} list with components:
\describe{
  \item{best_error}{Numeric. Minimum objective error achieved.}
  \item{data}{Data frame of optimized predictor and outcome values.}
  \item{optim_vec}{The \code{stats2data.object} returned by the internal
    \code{optim_vec} call (marginal optimization results).}
  \item{inputs}{List of all input parameters for reproducibility.}
  \item{track_error}{Numeric vector of best error at each iteration.}
  \item{track_error_ratio}{Numeric vector of error ratios (cor vs. reg)
    per iteration.}
}
}
\description{
Simulates data such that the
resulting correlations and regression coefficients match specified targets
under a given regression model. Internally calls \code{\link{optim_vec}} to
first generate marginals matching target means and standard deviations,
then optimizes predictor orderings via simulated annealing (and
hill climbing) to match correlation and regression targets.
}
\examples{
\dontrun{
res <- optim_mlr(
  N            = 100,
  target_mean  = c(X1 = 5, X2 = 3, Y = 10),
  target_sd    = c(X1 = 1, X2 = 2, Y = 3),
  range        = c(0, 20),
  integer      = FALSE,
  target_cor   = c(.23, .10, .45),
  target_reg   = c(2.1, 1.2, -0.8),
  reg_equation = "Y ~ X1 + X2",
  max_iter     = 10000,
  hill_climbs  = 50
)
}
}
