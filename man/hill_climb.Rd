% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_hill_climb.R
\name{hill_climb}
\alias{hill_climb}
\title{Perform hill-climbing optimization}
\usage{
hill_climb(
  current_candidate,
  error_function,
  N,
  hill_climbs = 100,
  num_preds = NULL,
  neighborhood_size = 4,
  progressor = NULL,
  pb_interval = NULL,
  progress_mode = "console"
)
}
\arguments{
\item{current_candidate}{Matrix. The predictor matrix to be optimized.}

\item{error_function}{Function. Objective function that takes a candidate and
returns a list with element \code{$total_error}.}

\item{N}{Integer. Number of observations (rows) in \code{current_candidate}.}

\item{hill_climbs}{Integer. Maximum number of iterations. Default \code{1e2}.}

\item{num_preds}{Integer. Number of predictor columns.}

\item{neighborhood_size}{Integer. Number of candidate moves evaluated per
iteration. Default \code{4}.}

\item{progressor}{Function or NULL. A \code{progressr} progressor callback.
Default \code{NULL}.}

\item{pb_interval}{Integer or NULL. Interval (in iterations) between
progressor calls. Default \code{NULL}.}

\item{progress_mode}{Character: \code{"console"}, \code{"shiny"}, or
\code{"off"}. Default \code{"console"}.}
}
\value{
A list with components:
\describe{
  \item{best_candidate}{The optimized candidate matrix achieving lowest error.}
  \item{best_error}{Numeric. The minimum objective function value found.}
}
}
\description{
Executes a hill-climbing algorithm to iteratively improve a candidate data set
by minimizing a supplied error function. Supports the LM module.
}
\examples{
\dontrun{
hill_climb(
  current_candidate = matrix(rnorm(200), 100, 2),
  error_function = function(cand) list(total_error = sum(cand^2)),
  N = 100,
  hill_climbs = 100,
  num_preds = 2
)
}
}
